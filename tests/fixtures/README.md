# Fixtures

`tailscale-status.json` is synthetic Tailscale status data for parser tests.
Machine names, addresses, node IDs and public node keys are placeholders. It is
not a runtime snapshot or evidence that these machines are reachable.
