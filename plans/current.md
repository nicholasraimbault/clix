# Current work

The accepted design is [Clix](../docs/superpowers/specs/2026-09-11-clix-design.md).
The v0 recipe is [the implementation plan](../docs/superpowers/plans/2026-09-11-clix-v0.md).

No pair, sidecar, grant, mesh exec, wait, pin, notification or two-machine
runtime proof exists. The crate is not started.

**Prepared milestone:** v0 on two Arch boxes — pair, `clix add adb` on the
laptop, `clix laptop adb` from the server, `bash` denied, lid down waits.
